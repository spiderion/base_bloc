name: base_bloc
description: Base bloc package.
version: 0.0.1
author: Ion Olaru
homepage: Base Bloc

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
